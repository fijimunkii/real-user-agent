name: 'Sync'
on:
  pull_request:
    paths-ignore:
      - '**.md'
  push:
    branches:
      - master

jobs:
  sync:
    name: Sync user agent
    runs-on: [ubuntu-latest]
    env:
      DOCKER_BUILDKIT: 1
    defaults:
      run:
        shell: bash
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Sync
      run: |
        ./sync.sh
